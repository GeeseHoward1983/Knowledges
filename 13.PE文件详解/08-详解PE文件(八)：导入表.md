---
title: "详解PE文件(八)：导入表"
series: PE文件详解
order: 8
source: https://blog.csdn.net/geesehoward20000/article/details/154742735
author: geesehoward20000（阿捏利）
published: 2025-11-19
collected: 2026-06-26
aliases:
  - 导入表
  - Import Table
  - IMAGE_IMPORT_DESCRIPTOR
  - PE(八)
summary: 导入表记录程序运行时需从外部 DLL 调用哪些函数。核心是 IMAGE_IMPORT_DESCRIPTOR 数组（每个 DLL 一项），通过 INT（名称）与 IAT（地址）双表完成动态链接。
tags:
  - PE文件
  - Windows
  - 逆向工程
  - 二进制文件格式
  - 数据目录
---
# 详解PE文件(八)：导入表

> 来源：[CSDN 原文](https://blog.csdn.net/geesehoward20000/article/details/154742735)　｜　发布：2025-11-19　｜　合集《PE文件详解》第 8/26 篇

> [!tip] 一句话核心
> 导入表回答一个问题：**这个程序要从哪些 DLL、借哪些函数？** 它的核心是一个 `IMAGE_IMPORT_DESCRIPTOR` 数组（**每个被导入的 DLL 一项**），每项又拉出两条并行的"清单"：**INT**（导入名称表，记函数名）和 **IAT**（[[20-详解PE文件(二十)：导入地址表|导入地址表]]，加载后被填入真实函数地址）。

> [!info] 速查
> - **数据目录索引**：1（`IMAGE_DIRECTORY_ENTRY_IMPORT`，见 [[07-详解PE文件(七)：数据目录|数据目录]]）
> - **核心结构**：`IMAGE_IMPORT_DESCRIPTOR`（以全零项结尾）
> - **配套结构**：`IMAGE_THUNK_DATA`（INT/IAT 元素）、`IMAGE_IMPORT_BY_NAME`（按名导入）
> - **关键概念**：INT（名字清单，恒定）vs IAT（地址清单，加载时回填）
> - **对偶篇**：[[09-详解PE文件(九)：导出表|导出表]]（一方导出、一方导入）

导入表记录了程序运行时需要从外部动态链接库（DLL）中调用的函数信息，使程序能复用系统或他人 DLL 的功能，而无需把这些代码塞进自身。

## 导入表概述

当程序需要调用外部 DLL 中的函数时，这些函数的名称和所在 DLL 的信息就存储在导入表中。Windows 加载器在加载程序时会根据导入表加载相应的 DLL，并将函数的实际地址填入 [[20-详解PE文件(二十)：导入地址表|导入地址表（IAT）]]。

## 导入表的整体结构

导入表不是一张表，而是"描述符 + 两条并行清单 + 函数名"的网状结构：

```text
DataDirectory[1] ─► IMAGE_IMPORT_DESCRIPTOR[]   (每个 DLL 一项，全零项结尾)
   ┌────────────────────────────┐
   │ OriginalFirstThunk (INT) ──┼─► [thunk][thunk]…0 ─► IMAGE_IMPORT_BY_NAME{Hint,"MessageBoxA"}
   │ Name ──────────────────────┼─► "user32.dll"
   │ FirstThunk (IAT) ──────────┼─► [thunk][thunk]…0  (加载后被覆盖为真实地址)
   └────────────────────────────┘
   ┌────────────────────────────┐
   │ ...下一个 DLL (kernel32.dll)│
   └────────────────────────────┘
```

### 1. 导入描述符数组（IMAGE_IMPORT_DESCRIPTOR）

导入表的核心是一个 `IMAGE_IMPORT_DESCRIPTOR` 结构数组，每个结构描述一个被导入的 DLL。数组以一个全零的结构作为结束标志。

```c
typedef struct _IMAGE_IMPORT_DESCRIPTOR {
    union {
        DWORD   Characteristics;            // 0 for terminating null import descriptor
        DWORD   OriginalFirstThunk;         // RVA to original unbound IAT (PIMAGE_THUNK_DATA)
    } DUMMYUNIONNAME;
    DWORD   TimeDateStamp;                  // 0 if not bound,
                                            // -1 if bound, and real date\time stamp
                                            //     in IMAGE_DIRECTORY_ENTRY_BOUND_IMPORT (new BIND)
                                            // O.W. date/time stamp of DLL bound to (Old BIND)

    DWORD   ForwarderChain;                 // -1 if no forwarders
    DWORD   Name;                           // RVA to DLL name
    DWORD   FirstThunk;                     // RVA to IAT (if bound this IAT has actual addresses)
} IMAGE_IMPORT_DESCRIPTOR;
```

各字段说明：

| 字段 | 含义 |
|---|---|
| `OriginalFirstThunk` | 指向**导入名称表（INT）**的 RVA |
| `TimeDateStamp` | 时间戳，用于[[19-详解PE文件(十九)：绑定配置表\|绑定导入]] |
| `ForwarderChain` | 转发链，用于处理函数转发（`-1` 表示无） |
| `Name` | DLL 名称字符串的 RVA |
| `FirstThunk` | 指向**导入地址表（IAT）**的 RVA |

### 2. 导入名称表（Import Name Table, INT）

INT 是一个 `IMAGE_THUNK_DATA` 结构数组，每个元素对应一个导入函数：

```c
typedef struct _IMAGE_THUNK_DATA32 {
    union {
        DWORD ForwarderString;      // PBYTE
        DWORD Function;             // PDWORD
        DWORD Ordinal;
        DWORD AddressOfData;        // PIMAGE_IMPORT_BY_NAME
    } u1;
} IMAGE_THUNK_DATA32;
```

### 3. 导入地址表（Import Address Table, IAT）

IAT 与 INT **结构完全相同**，但在程序加载时，加载器会把每个槽位替换为对应函数的**实际地址**。详见 [[20-详解PE文件(二十)：导入地址表]]。

### 4. 导入名称结构（IMAGE_IMPORT_BY_NAME）

对于按名称导入的函数，INT 中的元素指向此结构：

```c
typedef struct _IMAGE_IMPORT_BY_NAME {
    WORD    Hint;
    CHAR   Name[1];
} IMAGE_IMPORT_BY_NAME, *PIMAGE_IMPORT_BY_NAME;
```

`Hint` 是一个"提示序号"，帮助加载器更快在 [[09-详解PE文件(九)：导出表|导出表]] 中定位函数；`Name` 是变长函数名字符串。

> [!important] INT 与 IAT：为什么要两份一样的表
> 这是导入机制最关键的设计：
>
> | | INT（OriginalFirstThunk） | IAT（FirstThunk） |
> |---|---|---|
> | 加载**前** | 指向函数名 | 指向函数名（与 INT 相同） |
> | 加载**后** | **保持不变**（仍指向名字） | **被覆盖为真实函数地址** |
>
> 加载器拿 INT 当"原始清单"逐个解析函数地址，写进 IAT。程序运行时调用 `call [IAT槽]` 就能跳到真实函数。保留 INT 是为了在地址被改写后，仍有一份"名字底稿"可供[[19-详解PE文件(十九)：绑定配置表|绑定校验]]等用途。

## 导入表工作原理

```text
① 链接器在 EXE 里建好导入表（DLL 名 + 函数名清单）
        │
② 加载器读取导入表，逐个 IMAGE_IMPORT_DESCRIPTOR
        │
③ 按 Name 加载对应 DLL 到进程地址空间
        │
④ 对 INT 中每个函数：在该 DLL 的[[09-详解PE文件(九)：导出表|导出表]]里查地址 → 写入 IAT
        │
⑤ 运行时 call [IAT] 直接跳到真实函数
```

## 导入表在 PE 文件中的位置

导入表的位置和大小存储在 [[07-详解PE文件(七)：数据目录|可选头的数据目录]]中，索引为 `IMAGE_DIRECTORY_ENTRY_IMPORT`（值为 1）：

```c
// 数据目录中的导入表项
DataDirectory[IMAGE_DIRECTORY_ENTRY_IMPORT]
```

## 实际示例

以下是一个遍历 PE 文件导入表的示例代码：

```c
#include <windows.h>
#include <stdio.h>

void PrintImportTable(const char* filename) {
    HANDLE hFile = CreateFile(filename, GENERIC_READ, FILE_SHARE_READ,
                              NULL, OPEN_EXISTING, FILE_ATTRIBUTE_NORMAL, NULL);

    if (hFile == INVALID_HANDLE_VALUE) {
        printf("无法打开文件: %s\n", filename);
        return;
    }

    HANDLE hMapping = CreateFileMapping(hFile, NULL, PAGE_READONLY, 0, 0, NULL);
    if (!hMapping) {
        printf("创建文件映射失败\n");
        CloseHandle(hFile);
        return;
    }

    LPVOID lpBase = MapViewOfFile(hMapping, FILE_MAP_READ, 0, 0, 0);
    if (!lpBase) {
        printf("映射文件失败\n");
        CloseHandle(hMapping);
        CloseHandle(hFile);
        return;
    }

    // 获取DOS头
    IMAGE_DOS_HEADER* dosHeader = (IMAGE_DOS_HEADER*)lpBase;

    // 验证DOS签名
    if (dosHeader->e_magic != IMAGE_DOS_SIGNATURE) {
        printf("不是有效的PE文件\n");
        UnmapViewOfFile(lpBase);
        CloseHandle(hMapping);
        CloseHandle(hFile);
        return;
    }

    // 获取NT头
    IMAGE_NT_HEADERS* ntHeaders = (IMAGE_NT_HEADERS*)((BYTE*)lpBase + dosHeader->e_lfanew);

    // 验证PE签名
    if (ntHeaders->Signature != IMAGE_NT_SIGNATURE) {
        printf("不是有效的PE文件\n");
        UnmapViewOfFile(lpBase);
        CloseHandle(hMapping);
        CloseHandle(hFile);
        return;
    }

    // 获取导入表信息
    IMAGE_DATA_DIRECTORY* importDir = &ntHeaders->OptionalHeader.DataDirectory[IMAGE_DIRECTORY_ENTRY_IMPORT];
    if (importDir->VirtualAddress == 0) {
        printf("该文件没有导入表\n");
        UnmapViewOfFile(lpBase);
        CloseHandle(hMapping);
        CloseHandle(hFile);
        return;
    }

    // 获取导入描述符数组
    IMAGE_IMPORT_DESCRIPTOR* importDesc = (IMAGE_IMPORT_DESCRIPTOR*)((BYTE*)lpBase + importDir->VirtualAddress);

    printf("=== 导入表信息 ===\n");

    // 遍历导入描述符数组
    for (; importDesc->Name != 0; importDesc++) {
        // 获取DLL名称
        char* dllName = (char*)((BYTE*)lpBase + importDesc->Name);
        printf("\nDLL名称: %s\n", dllName);

        // 获取导入名称表
        IMAGE_THUNK_DATA32* thunk = (IMAGE_THUNK_DATA32*)((BYTE*)lpBase + importDesc->OriginalFirstThunk);

        // 遍历导入函数
        while (thunk->u1.AddressOfData != 0) {
            if (thunk->u1.Ordinal & IMAGE_ORDINAL_FLAG32) {
                // 按序号导入
                printf("  函数序号: %d\n", IMAGE_ORDINAL32(thunk->u1.Ordinal));
            } else {
                // 按名称导入
                IMAGE_IMPORT_BY_NAME* importByName = (IMAGE_IMPORT_BY_NAME*)((BYTE*)lpBase + thunk->u1.AddressOfData);
                printf("  函数名称: %s (Hint: %d)\n", importByName->Name, importByName->Hint);
            }
            thunk++;
        }
    }

    UnmapViewOfFile(lpBase);
    CloseHandle(hMapping);
    CloseHandle(hFile);
}
```

> [!note] 注意这里用 INT 而非 IAT 遍历
> 示例里遍历的是 `OriginalFirstThunk`（INT）。因为静态分析磁盘文件时，IAT 还没被加载器回填，用 INT 才能稳定拿到函数名。最高位是 `IMAGE_ORDINAL_FLAG32` 时表示"按序号导入"，否则按名称导入。

## 导入表的重要性

| 价值 | 说明 |
|---|---|
| **模块化设计** | 复用外部库功能，无需把代码全打进 EXE |
| **节省内存** | 多个程序共享同一 DLL |
| **便于更新** | 更新 DLL 即可影响所有使用者 |
| **逆向工程** | 看导入表即可快速判断程序"会调用哪些系统能力" |
| **安全分析** | 程序调用的关键 API（网络、进程、注册表等）都体现在导入表里 |

## 按序号导入与 Import 混淆

### 按序号导入（Ordinal-Only Import）

前面代码示例中已出现 `IMAGE_ORDINAL_FLAG`，这里深入原理。

当 INT 中某个 `IMAGE_THUNK_DATA` 的**最高位**被置位时，该函数不按名字查找，而是直接按序号导入：

```c
// 32 位
#define IMAGE_ORDINAL_FLAG32  0x80000000UL
#define IMAGE_ORDINAL32(Ordinal) ((Ordinal) & 0xFFFF)

// 64 位
#define IMAGE_ORDINAL_FLAG64  0x8000000000000000ULL
#define IMAGE_ORDINAL64(Ordinal) ((Ordinal) & 0xFFFF)
```

**检测方法（伪代码）：**

```c
IMAGE_THUNK_DATA32 *thunk = (IMAGE_THUNK_DATA32*)INT;
while (thunk->u1.AddressOfData != 0) {
    if (thunk->u1.Ordinal & IMAGE_ORDINAL_FLAG32) {
        WORD ordinal = IMAGE_ORDINAL32(thunk->u1.Ordinal);
        // 只有序号，没有函数名字——逆向时只能查 DLL 的导出表对照
        printf("Ordinal: %u\n", ordinal);
    } else {
        IMAGE_IMPORT_BY_NAME *name = (IMAGE_IMPORT_BY_NAME*)(base + thunk->u1.AddressOfData);
        printf("Name: %s (Hint: %u)\n", name->Name, name->Hint);
    }
    thunk++;
}
```

**逆向识别要点：**

| 现象 | 含义 |
|---|---|
| INT thunk 最高位为 1 | 按序号导入，无函数名 |
| 字符串区域中没有对应名字 | 也是按序号导入的信号 |
| 只有序号值（如 `#231`） | 需查目标 DLL 的 EAT，用序号 - Base 定位函数 |

按序号导入在 COM 组件（`oleaut32.dll`）、内部 DLL（`ntdll.dll` 与 `ntoskrnl.exe` 之间）和某些刻意隐藏 API 名的壳中极为常见。CFF Explorer 会在导入表视图中以 `Ordinal 231` 等形式直接标注；IDA 则会尝试自动从 FLIRT 签名或加载的 DLL 还原函数名。

```text
dumpbin /imports target.exe
```

输出中形如 `231` 而非函数名的条目即为序号导入。

---

### API 哈希混淆（Hash-based Dynamic Resolution）

恶意软件（Shellcode、无文件攻击、加壳程序）为隐藏所使用的 API，常不留任何明文 Import 表，而在运行时动态解析：

**经典流程：**

```text
1. 枚举 PEB.Ldr 链表中所有已加载 DLL
2. 遍历每个 DLL 的导出表（ENT）
3. 对每个函数名计算哈希
4. 与预埋的目标哈希列表比对
5. 命中则调用 GetProcAddress（或直接读 EAT[ordinal]）获取地址
```

**常见哈希算法：**

| 算法 | 特征 | 说明 |
|---|---|---|
| ROR13 | `hash = (hash >>> 13) \| (hash << 19); hash += char` | Metasploit / Cobalt Strike shellcode 默认 |
| DJB2 | `hash = hash * 33 ^ char` | 计算简单，碰撞率较高 |
| CRC32 | 标准 CRC32 多项式 | 速度快，IDA 可直接识别多项式常数 |
| 自定义 | 各 APT 组织变体 | 逆向时先识别循环结构再提取常数 |

**ROR13 哈希示例（x86 Shellcode 汇编风格）：**

```asm
; esi = 指向函数名字符串
; ebx 累计哈希
xor  ebx, ebx
.hash_loop:
    movsx eax, byte [esi]
    test  al, al
    jz    .done
    ror   ebx, 0Dh          ; 右循环移位 13
    add   ebx, eax
    inc   esi
    jmp   .hash_loop
.done:
; ebx = ROR13 哈希值，与预埋常数比较
cmp  ebx, 0C8AC8026h        ; LoadLibraryA 的 ROR13 哈希
```

**逆向识别哈希查找的关键特征：**

1. 代码中出现遍历 `PEB.Ldr` 或 `PEB_LDR_DATA.InMemoryOrderModuleList` 的链表遍历
2. 紧跟着嵌套循环遍历 EAT（`IMAGE_EXPORT_DIRECTORY` 三张表）
3. 循环体内出现位旋转（`ror`/`rol`）或乘法（`imul`/`*33`）再 `xor`/`add` 字符
4. 与魔数（如 `0xC8AC8026`）比较而非与字符串比较
5. 导入表中只有 `GetProcAddress`/`LoadLibraryA` 等最基础 API，其余均缺失

**工具辅助：**

```python
# Python 快速还原 ROR13 哈希对应的函数名（离线枚举）
import ctypes, os

def ror13(val, bits=32):
    return ((val >> 13) | (val << (bits - 13))) & 0xFFFFFFFF

def hash_name(name: bytes) -> int:
    h = 0
    for b in name:
        h = ror13(h)
        h = (h + b) & 0xFFFFFFFF
    return h

target = 0xC8AC8026
# 遍历 Windows 系统 DLL 导出名查找命中
```

IDA 插件 `HashDB`（odb.haruspex.io）维护了数千个已知哈希值的映射表，可直接标注常量。

---

### 延迟解析（Lazy Binding via Delay Import）

标准导入表在进程启动时全量解析所有 IAT 项。延迟导入（`Delay-Load Import`）将解析推迟到函数**首次被调用时**，减少启动时间并允许在目标 DLL 不存在时优雅降级。详细结构见 [[21-详解PE文件(二十一)：延迟导入表]]。

与 API 哈希混淆的关键区别：

| 维度 | 延迟导入 | API 哈希混淆 |
|---|---|---|
| 结构可见性 | PE 数据目录 [13] 有记录，工具可枚举 | 无任何 PE 结构记录，完全运行时动态 |
| 函数名可见 | 链接器在 `.didat` 节保留函数名字符串 | 无函数名，只有哈希常数 |
| 目的 | 性能优化/可选功能 | 主动隐藏 API 使用，反分析 |
| 逆向难度 | 工具直接显示 | 需识别算法+暴力枚举还原 |

> [!summary] 小结
> 导入表以 `IMAGE_IMPORT_DESCRIPTOR` 数组组织每个依赖 DLL，通过 **INT（名字）+ IAT（地址）** 双表完成动态链接：加载器顺着 INT 在目标 DLL 的 [[09-详解PE文件(九)：导出表|导出表]]里查地址、回填 [[20-详解PE文件(二十)：导入地址表|IAT]]。它与导出表是一对"借/还"关系，是逆向分析中最先要看的结构之一；其变体还有 [[19-详解PE文件(十九)：绑定配置表|绑定导入]] 与 [[21-详解PE文件(二十一)：延迟导入表|延迟导入]]。恶意软件常通过**按序号导入**隐藏函数名、通过 **API 哈希混淆**彻底清空导入表——理解这两种手法是恶意代码分析的基本功。

---

← 上一篇：[[07-详解PE文件(七)：数据目录]]　｜　[[00-合集总览-PE文件详解|📚 返回合集总览]]　｜　下一篇：[[09-详解PE文件(九)：导出表]] →
